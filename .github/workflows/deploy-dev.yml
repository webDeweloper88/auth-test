name: Deploy to Dev

on:
  push:
    branches:
    - dev

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Trigger Coolify Deploy (Dev)
      run: |
        curl -X POST \
          "${{ secrets.COOLIFY_WEBHOOK_URL_DEV }}"
